# Generated data directories (private, never commit)
data/
!data/.gitkeep

# Archive patches (local only, may contain paths)
archive/

# Private configuration (contains source-company mappings, secrets)
configs/company.yaml
configs/*.local.yaml

# Environment files (secrets)
.env
.env.*
*.env
*.key
*.secret
*.pem
*.p12

# Python cache and build artifacts
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
.venv/
.env/

# Lock files (not using pipenv/poetry/uv lock)
Pipfile.lock
poetry.lock
uv.lock

# IDE and editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
.mypy_cache/
.ruff_cache/

# Type checking
*.pyi
stubs/

# Logs
*.log
logs/

# Model weights and embeddings (never commit)
*.bin
*.pt
*.pth
*.onnx
*.safetensors
embeddings/
models/
xet/
*.agent_harnesses.json

# Generated artifacts (never commit)
artifacts/
anonymized_bundle/
exports/
MagicMock/
originals/
output/
private_maps/
smoke_excerpts/
*.zip
*.parquet
*.jsonl
*.json
!tests/fixtures/**/*.json

# Checkpoints (can be regenerated)
.checkpoints/

# Notebooks — track the primary notebook, ignore generated notebooks and checkpoints
notebooks/*
!notebooks/FENRIX_MultiCompany_Anonymization.ipynb
.ipynb_checkpoints/
*.ipynb
!notebooks/FENRIX_MultiCompany_Anonymization.ipynb
!notebooks/FENRIX_MultiCompany_Anonymization_Reproducible.ipynb

# Private identity registries (never commit)
private/
private/**/*

# Sanitized evidence artifacts (never commit — generated on demand)
evidence/

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
Scott.zip
scripts/*
!scripts/build_submission_fast.py

# professor_alpha_v2 is a local reference only — never tracked (contains seeded test identifiers)
src/fenrix_synthetic/professor_alpha_v2.py
tests/unit/test_professor_alpha_v2.py
scripts/build_professor_alpha_v2.py
tests/fixtures/sec_leak_examples/
