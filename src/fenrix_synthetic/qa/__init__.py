"""QA modules for V3 release boundary.

- direct_identifier_scan: scans for direct identifiers (CIK, ticker, etc.)
- metadata_scan: scans for SEC/iXBRL metadata artifacts
- release_gate: strict fail-closed release gate aggregating all scans
"""
