# Campaign configuration
# Uses canary placeholder — real company ID belongs in gitignored private config.
company_id: "CANARY"
stages:
  - "ingest"
  - "extract"
  - "manifest"
resume: true
stop_on_failure: true
config_overrides: {}
